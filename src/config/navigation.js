/**
 * Primary navbar links (desktop + mobile menu).
 * Leave empty to hide the nav cluster; add entries when you need top-level links.
 *
 * @example
 * export const PRIMARY_NAV = [
 *   { id: "home", label: "Home", to: "/" },
 * ];
 */
export const PRIMARY_NAV = [];
